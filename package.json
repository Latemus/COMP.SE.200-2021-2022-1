{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha './test/**.js' --exit",
    "testexit": "mocha --exit",
    "coveralls": "npm run coverage -- --report lcovonly && cat ./coverage/lcov.info | coveralls",
    "coverage": "istanbul cover npm run test --no-default-excludes --report lcovonly -- -R spec"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "coveralls": "^3.1.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0"
  }
}
